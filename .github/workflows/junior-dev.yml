# STUB — placeholder pending KAN-2 (Build Junior Developer agent).
# The orchestrator (KAN-1) dispatches this workflow for easy tickets
# (story points 1-3). Until KAN-2 lands, this just echoes the input.
name: Junior Developer agent (stub)

on:
  workflow_dispatch:
    inputs:
      ticket_key:
        description: 'Jira ticket key (e.g. KAN-42)'
        required: true
        type: string

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - name: Acknowledge dispatch
        env:
          TICKET_KEY: ${{ inputs.ticket_key }}
        run: echo "junior-dev stub invoked for ${TICKET_KEY}"
